# Data

Place the project Excel workbook in this folder using the following filename:

`Ad_Spend_Budget_Performance_Analytics.xlsx`

The Python script expects the workbook to contain these sheets:

- `Budget vs Actual`
- `Ad Channel Performance`

The analysis reads the workbook directly and generates summaries and charts in the `outputs/` directory.
